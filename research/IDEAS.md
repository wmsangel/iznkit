# Идеи — инбокс (iznkit.com)

Свободный список идей по проекту. Кидай сюда что угодно одной строкой или
абзацем — без оформления. Это переживает сессии, и дневной авто-аналитик читает
файл: если в `research/PLAN.md` не осталось задач, он может взять идею отсюда,
оформить и двинуть в работу (крупное/рискованное — сначала уточнит у владельца).

Формат (необязательно): `- [ ] идея — короткий контекст/зачем`.
Взятое в работу помечай `[x]` или переноси в `research/PLAN.md`.
Смотри также память проекта (`~/.claude/projects/.../memory/`) — второй дом идей.

## Из конкурентного аудита (2026-09-24)

Полный разбор — в памяти `competitive-research`. Правило монетизации: НЕ подписки
(КГ, без Stripe). Модель = Omni Calculator (AdSense при трафике) + контекстные
аффилиаты (бух-SaaS/аналитика маркетплейсов/банки для ИП на инвойс- и
маркетплейс-страницах). One-time «unlock PDF» оставляем, но не опираемся (в RU
бесплатный PDF/Word — норма). Все идеи ниже — client-side, без бэкенда.

### A. Маркетплейс — выигрываем ДАННЫМИ (наша горячая ниша, высший приоритет)
- [ ] Таблицы комиссий по категориям WB/Ozon/Яндекс Маркет — статический JSON,
      огромный длинный хвост («комиссия Ozon категория X»). ⚠️ Ставки МЕНЯЮТСЯ:
      обязательно с датой и оговоркой «проверьте в кабинете», как в гайде FBO/FBS.
- [ ] Апгрейд маркетплейс-калькуляторов: различие FBO/FBS, хранение ₽/литр/день,
      логистика по объёму/складу.
- [ ] Налоговые режимы РФ в расчётах: УСН 6/15, АУСН, НДС (вх./исх.), НПД.

### B. Dev-инструменты (дёшево, client-side, чёткий спрос; по 6-файловому паттерну)
Уже есть (НЕ дублировать): case-converter, lorem, password-generator, color,
cron-parser, regex, jwt, text-diff, json↔csv, base64, hash, uuid, url-encode,
timestamp, css-gradient, qr, wifi-qr, json-formatter.
- [ ] JSON → TypeScript / Go типы — главный магнит transform.tools.
- [ ] YAML ↔ JSON конвертер.
- [ ] Markdown → HTML preview.
- [ ] SQL formatter / beautifier.
- [ ] Minify + beautify (HTML/CSS/JS).
- [ ] Image → Base64 (data-URI) — через FileReader, без загрузки на сервер.
- [ ] Палитра/оттенки цвета — расширение существующего color.
- [ ] chmod-калькулятор; XML/YAML formatter; cron-builder (обратный к parser).

### C. RU «закрывающие документы» (сильный RU-спрос; каждый = отдельный лендинг)
Тяжелее калькулятора (нужен @react-pdf), но в нашем профиле генераторов.
- [ ] УПД, ТОРГ-12, акт сверки, счёт-фактура.
- [ ] Конструктор договора / КП.
- [ ] ГОСТ-QR оплаты на инвойсе + экспорт в Word/Excel (не только PDF) — базовое
      ожидание RU-пользователя.

### D. Ширина и монетизация
- [ ] Точечно добавлять калькуляторы длинного хвоста (стройка/быт/здоровье) —
      дёшево, как allcalc/Omni.
- [ ] Новый аффилиат-вектор: бух-SaaS (МойСклад), аналитика маркетплейсов, банки
      для ИП — контекстно на инвойс/маркетплейс-страницах (аудитория перегрета
      такой рекламой). Добавить в `src/lib/affiliates.ts` при наличии программ.

### E. UX/формат «украсть» (беспроигрышно)
- [ ] Заметная строка «данные не покидают браузер» на dev- и RU-страницах.
- [ ] «Без регистрации» trust-строка.
- [ ] Избранное/пиннинг инструментов; PWA «на домашний экран».

## F. Аналитика и инструментация (обвесить метриками — видеть, что популярно)
Цель: у каждого значимого действия — событие GA4 + (где важно) key event/цель,
чтобы решать бэклог по фактам (что смотрят, куда жмут, чем пользуются), а не на глаз.
Всё через существующий `track()` (`src/lib/analytics.ts`); GA прод-only + Consent Mode.
- [ ] Полное покрытие событий: каждый инструмент шлёт осмысленный `tool_use`
      (`calculate`/`copy`/`download`/`share`/`generate`). Сейчас часть — только
      page_view (loan/tip/pct/regex — см. GA4-заметку в памяти). Добить все.
- [ ] **Клики по аффилиатам** — `affiliate_click {slot, partner, tool}` на ссылках
      `AffiliateSlot` (`src/components/affiliate-slot.tsx`). Критично: без этого не
      видно CTR монетизации. Плюс общий `outbound_click` на внешние ссылки.
- [ ] Внутренняя навигация как события: клики related-tools, tool→guide и guide→tool
      CTA, category-hub, шаринг (ShareLink уже шлёт `tool_use:share`), FAB
      (`fab_action` уже есть), донат (`copy_donate_address`/`donate_share` уже есть).
- [ ] Поиск на сайте: `site_search {has_results}` (длина запроса/был ли результат).
- [ ] GA4-конфиг (действие владельца в дашборде): зарегистрировать event-scoped
      custom dimensions `tool`, `action`, `slot`, `partner`; отметить key events
      (цели): `affiliate_click`, `fab_action=support`, ключевые `tool_use`.
- [ ] Отчёт/Explore в GA4: топ-инструменты по `tool_use`, разбивка по `action`,
      CTR аффилиатов, вовлечённость гайдов. Дополняем cf_stats.py (топ-страницы/
      referrers без cookie-согласия).
- [ ] Петля решений: раз в период сверять GA (топ tool_use/action) + GSC → двигать
      бэклог по реальному спросу (усиливать популярное, чинить high-impression/
      low-CTR). Кормить дневного аналитика.

## Идеи (свободные)

- (пусто — добавляй сюда)
